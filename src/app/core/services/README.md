# Core Services

This folder is reserved for singleton core services (e.g., `EmployeeService`, `AnalyticsService`, `NotificationService`) that will handle future REST API interactions with the Spring Boot backend.

No API calls or fake data services are implemented in this foundation step.
